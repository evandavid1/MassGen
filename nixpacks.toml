[phases.setup]
nixPkgs = ["python311", "uv", "nodejs_20"]

[phases.install]
cmds = ["uv venv .venv", "uv pip install . --python .venv/bin/python"]

[start]
cmd = ".venv/bin/python -m massgen.cli --web --web-host 0.0.0.0 --web-port $PORT"
