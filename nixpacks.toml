[phases.setup]
nixPkgs = ["python311", "nodejs_20"]
cmds = ["python -m pip install uv"]

[phases.install]
cmds = ["uv pip install . --system"]

[start]
cmd = "python -m massgen.cli --web --web-host 0.0.0.0 --web-port $PORT"
